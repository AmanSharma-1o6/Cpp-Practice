#include<iostream>
using namespace std;
int main(){
    int a=4, b=5;
    cout<<(a==b);
    // as it is false so output must be 0
    cout<<(a!=b);
    // as it is truee so output must be 1
    cout<<(a>b);
    // true output 1
    cout<<(a<b);
    // false output 0
    return 0;
}